# Compliant reference case for the three-state screening suite.
# Analytic surrogate; not CFD or field truth.
name: brinewatch_screening_clear
seed: 23
planner: lawnmower
environment:
  ambient_salinity_psu: 38.8
  salinity_stratification_per_m: 0.002
  ambient_temperature_c: 24.0
  seabed_z0: -34.0
  current_dir_deg: 0.0
  tide_amplitude_m: 0.0
outfall:
  x: 30.0
  y: 0.0
  axis_deg: 0.0
  n_ports: 6
  port_spacing_m: 3.2
  riser_height_m: 1.35
  discharge_salinity_psu: 62.0
plume:
  nearfield_peak_anomaly_psu: 7.0
  farfield_peak_anomaly_psu: 3.5
  farfield_initial_width_m: 4.5
  farfield_spread_rate: 0.12
  layer_thickness_m: 1.1
  dilution_length_m: 24.0
survey:
  x_min: 18.0
  x_max: 78.0
  y_min: -28.0
  y_max: 28.0
  altitude_m: 1.0
  grid_resolution_m: 2.5
budget:
  max_distance_m: 950.0
  checkpoints: [1.0]
  locate_fraction: 0.05
lawnmower:
  line_spacing_m: 4.5
  along_x: true
gp:
  length_xy_m: 8.5
  length_z_m: 1.25
  signal_sigma_psu: 2.2
  noise_sigma_psu: 0.05
  max_train_points: 1200
compliance:
  mixing_zone_radius_m: 34.0
  threshold_increment_pct: 5.0
  eval_altitude_m: 0.8
  p_exceed_min: 0.65
  # The compliant reference has a 1.22 PSU reconstructed safety margin.
  # A 0.15 CLEAR probability bound is stated explicitly for this screening
  # policy; it is not retrofitted silently after evaluation.
  p_clear_max: 0.15
  max_posterior_std_psu: 0.85
backend:
  name: kinematic
